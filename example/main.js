import stickyHeader from '../dist/index.js';

console.log(stickyHeader);

document.addEventListener(
  'DOMContentLoaded',
  () => {
    const tableWrapper = document.querySelector('.table-wrapper');
  },
  false
);
